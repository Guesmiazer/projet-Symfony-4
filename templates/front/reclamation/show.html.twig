{% extends 'base.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block content %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr>
            <tr>
                <th>IdUtl</th>
                <td>{{ reclamation.idUtl }}</td>
            </tr>
            <tr>
                <th>Obj</th>
                <td>{{ reclamation.obj }}</td>
            </tr>
            <tr>
                <th>Rec</th>
                <td>{{ reclamation.rec }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'idRec': reclamation.idRec}) }}">edit</a>

    {{ include('front/reclamation/_delete_form.html.twig') }}
{% endblock %}
