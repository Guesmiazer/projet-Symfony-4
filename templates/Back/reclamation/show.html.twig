{% extends 'admin.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr>
            <tr>
                <th>IdUtl</th>
                <td>{{ reclamation.idUtl }}</td>
            </tr>
            <tr>
                <th>Obj</th>
                <td>{{ reclamation.obj }}</td>
            </tr>
            <tr>
                <th>Rec</th>
                <td>{{ reclamation.rec }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_reclamation_index') }}">back to list</a>

    <a href="{{ path('back_reclamation_edit', {'idRec': reclamation.idRec}) }}">edit</a>

    {{ include('back/reclamation/_delete_form.html.twig') }}
{% endblock %}
